server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
    location /search {
        proxy_pass http://backend:3000;
    }
    location /health {
        proxy_pass http://backend:3000;
    }
}
